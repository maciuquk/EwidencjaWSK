@model IEnumerable<EwidencjaWSK.Models.AdditionalDoc>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KindOfDoc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KindOfDoc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AdditionalDocId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AdditionalDocId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AdditionalDocId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
