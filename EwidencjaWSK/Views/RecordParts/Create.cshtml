@*@model EwidencjaWSK.Models.RecordPart*@
@model EwidencjaWSK.ViewModel.RecordPartsViewModel

@{
    ViewData["Title"] = "Dodawanie części do kontraktu";
}

<h1>Dodaj części do kontraktu nr @Model.Record.Number</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        @*<form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <div class="form-group">
                <label asp-for="PartId" class="control-label"></label>
                <select asp-for="PartId" class ="form-control" asp-items="ViewBag.PartId"></select>
            </div>

            <div class="form-group">
                <input type="submit" value="Zapisz" class="btn btn-primary" />
            </div>
        </form>*@

        <form asp-action="Create" asp-method="post">

            <input type="hidden" name="RecordId" value=@Model.Record.RecordId />
            <label></label>
            @*<input name="PartId" value="4" />*@
            <div class="form-group">
            <select name="PartId" class="form-control" asp-items="@(new SelectList(Model.Parts,"PartId","Name"))"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Zapisz" class="btn btn-primary" />
            </div>
</form>


    </div>
</div>

<div>
    <a asp-controller="Records" asp-action="Details" asp-route-id=@Model.Record.RecordId >Powrót do kontraktu</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
